import React from 'react';

import { Route } from '../../models/index';

export default function RestaurantsList ({ route } : { route : Route }) {
  const category = route.params;

  const restaurantsToPaint;
}
